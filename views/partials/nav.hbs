<style>
    .user-icon {
        font-size: 35px;
        font-weight: bold;
    }

    .list-item-link {
        font-weight: 500;
    }
    .list-item-link .nav-link:hover {
        background-color: rgba(255, 255, 255, 0.1);
        border-radius: 5px;
        color: #ffc107 !important;
        transition: background-color 0.3s ease, color 0.3s ease;
    }

    .btn-logout {
    background-color: #000;
    color: white;
    border: 1px solid white;
    border-radius: 5px;
    padding: 6px 20px;
    margin-right: 50px;
    transition: all 0.3s ease;
    font-weight: 500;
    height: 36px;
    font-size: 14px;
    }

    .btn-logout:hover,
    .btn-logout:focus,
    .btn-logout:active {
        background-color: #dc3545 !important;
        color: #fff !important;
        border-color: #dc3545 !important;
        box-shadow: 0 0 10px rgba(220, 53, 69, 0.5);
        transform: translateY(-1px);
        outline: none !important;
    }

    .navbar-brand {
        font-size: 30px;
        font-weight: 500;
        font-family: 'Poppins', sans-serif;
        background: url('/images/1.jpg') center;
        background-size: cover;
        -webkit-background-clip: text;
        background-clip: text;
        -webkit-text-fill-color: transparent;
        color: transparent;
        animation: animateGradient 10s linear infinite;
    }

    .navbar-brand i {
        font-size: 30px; 
    }

    @keyframes animateGradient {
        to {
            background-position-x: -200px;
        }
    }

</style>
<nav class="navbar navbar-expand-lg navbar-light fixed-top "style= "background-color:#343a40" >
    <div class="container-fluid d-flex justify-content-between align-items-center">
        {{#if user}}
            <a class="navbar-brand" style="margin-left: 50px;" href="/page/home">
                <i class="bi bi-globe mx-3"></i>
                Dealio
            </a>
        {{else}}
            <a class="navbar-brand" style="margin-left: 50px;" href="/">
                <i class="bi bi-globe mx-3"></i>
                Dealio
            </a>
        {{/if}}
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" title="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav" style="">
            <ul class="navbar-nav ms-auto" style="">
                {{#if user}}
                    <li class="nav-item d-flex justify-content-center align-items-center" style="">
                        <ul class="d-flex align-items-center gap-4 p-0 mb-0 ms-auto list-item-link" style="list-style: none; margin-right: 30px;">
                            <li><a href="/page/home" class="nav-link text-light">Trang chủ</a></li>
                            {{!-- <li><a href="#" class="nav-link text-dark">Liên hệ</a></li> --}}
                            <li><a href="/page/about" class="nav-link text-light">Giới thiệu</a></li>
                        </ul>
                        {{!-- <a href="#" style="color: black; text-decoration: none; display: flex; align-items: center;">
                                <i class="bi bi-person mx-3 user-icon w-25  "></i> --}}
                        </a>
                        <a class="nav-link btn-logout d-flex justify-content-center align-items-center" 
                            {{!-- style="background-color: black; width: 150px; height: 30px; color: white;
                                border-radius: 5px; margin-right: 30px; "  --}}
                            href="/page/logout"
                            >
                            <i class="bi bi-box-arrow-right me-2"></i> Đăng xuất
                        </a>
                    </li>
                {{else}}
                    <div class="d-flex align-items-center gap-4 p-0 mb-0 ms-auto list-item-link" style="list-style: none; margin-right: 30px;">
                        <li class="nav-item">
                            <a class="nav-link" style="color: white;" href="/page/login">Đăng nhập</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" style="color: white;" href="/page/register">Đăng ký</a>
                        </li>
                    </div>
                    
                {{/if}}
            </ul>
        </div>
    </div>
</nav>

<script>
document.addEventListener('DOMContentLoaded', function() {
    const logoutBtn = document.querySelector('.btn-logout');
    if (logoutBtn) {
        logoutBtn.addEventListener('click', function(e) {
            e.preventDefault();
            Swal.fire({
                title: 'Bạn có chắc muốn đăng xuất?',
                icon: 'warning',
                showCancelButton: true,
                confirmButtonText: 'Đăng xuất',
                cancelButtonText: 'Hủy',
                confirmButtonColor: '#d33',
                cancelButtonColor: '#3085d6'
            }).then((result) => {
                logoutBtn.blur();
                if (!result.isConfirmed) {
                    logoutBtn.style.display = 'none';
                    setTimeout(() => {
                        logoutBtn.style.display = '';
                    }, 10);
                }
                if (result.isConfirmed) {
                    window.location.href = logoutBtn.href;
                }
            });
        });
    }
});
</script>
